The folder `older` has the outs of the exact code initially provided. The folder `test` shows the outs of the refactored code.

As a result, it is evident that the refactored code has no numerical difference than the initial one.